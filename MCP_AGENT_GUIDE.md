# discord-local-ai-hub MCPサーバー 利用ガイド(AIエージェント向け)

このドキュメントは、Tailnet内の別端末で動く**Claude Code(またはその他のMCPクライアント)が
このMCPサーバーを実際に使う際に読む**ことを想定した利用ガイドです。セットアップ手順など
運用者向けの詳細は[README.md](./README.md)の「MCPサーバー機能」セクションを参照してください。

## これは何か

自宅のミニPC(api-relay)上で常時稼働しているDiscord AI botが、同一プロセス内でMCP
(Model Context Protocol)サーバーとしても機能を公開しています。Tailnet(Tailscale VPN)内に
いれば、Claude Codeから直接ローカルLLM・クラウドAI(Gemini/Grok/GPT-5.6)を呼び出せます。

主な用途: 今のセッションの文脈を持ち越す必要のない、独立した単発の質問や軽いコード生成を
外部委任し、自分自身のコンテキスト消費を抑える。

## 接続方法

```bash
claude mcp add --transport http discord-ai-hub http://100.96.65.109:5100/mcp
```

- 自分の端末がTailnetに参加していないと到達不可(`tailscale status`で確認)
- 接続できているかは `claude mcp list` で `✔ Connected` を確認

## 提供ツール: `ask(prompt, model?, session_id?, effort?, temperature?)`

一問一答ツールです。画像・ファイル添付には対応していません(テキストのみ)。

`session_id`を省略した場合は毎回完全に独立したリクエスト(履歴なし)。`session_id`を
指定すると、**同じIDでの呼び出し間だけ短期履歴を保持**します(有効期限20分、最大20往復、
プロセスメモリ上のみでサーバー再起動でも消える使い捨ての履歴。Discordのスレッド履歴とは
別物でSQLiteには保存されません)。複数ターンにわたる調査・監査タスクでは、タスクごとに
一意な`session_id`(例: `audit-20260910-repo-x`)を決めて使い続けてください。

### 引数

| 引数 | 必須 | 内容 |
|---|---|---|
| `prompt` | ○ | モデルへの質問・依頼内容(テキスト) |
| `model` | - | 使用するモデルID。省略(`null`)時はローカルの既定モデルを使用 |
| `session_id` | - | 短期履歴を紐付ける任意の文字列。省略時は履歴なし(単発リクエスト) |
| `effort` | - | 思考の深さ。`None`/`Low`/`Medium`/`High`/`XHigh`のいずれか。省略時は`Medium`。モデルが非対応の段階を指定しても自動調整される。不正な値は明確なエラーになる |
| `temperature` | - | 生成のランダム性(目安0.0〜2.0)。省略時は0.7。**OpenAIモデル(`gpt-5.6-*`)ではAPI仕様上デフォルト値以外を受け付けないため無視される**(エラーにはならない) |

### コンテキスト超過時の挙動

`session_id`の履歴+今回の`prompt`の合計トークン数が、選択したモデルのコンテキスト
上限(安全マージン込み)を超える場合、**黙って履歴を間引くのではなく明確なエラーを返し
ます**。エラーメッセージに推定トークン数と上限が含まれるので、新しい`session_id`で
仕切り直すか、プロンプトを短くして再試行してください。

### 応答が不完全な場合の挙動(出力上限・空応答)

モデルが正常に書き終えなかった場合、**途中までの本文を黙って返すことはしません**。

- **途中で終わった場合**: 本文の末尾に次のような注記が付きます。`finish_reason`
  (`length`/`MAX_TOKENS`は出力上限に到達、`content_filter`/`SAFETY`等はそれ以外の理由)と
  トークン内訳が含まれます

  ```
  [⚠ 応答は出力上限に達して途中で切れています(finish_reason=length、出力トークン=32000、推論トークン=12000、出力上限=32000)。続きが必要な場合は、同じsession_idで続きを依頼するか、質問を分割してください。]
  ```

- **本文が空の場合**: エラー(`isError: true`)になり、理由(`finish_reason`・トークン内訳)が
  メッセージに含まれます。空の応答は`session_id`の履歴にも残りません
- 途中で終わった応答は、同じ`session_id`で「続きを」と依頼できるよう本文だけを履歴に残します
  (注記は残しません)。ただし推論モデルは続きの生成でも改めて推論にトークンを使うため、
  確実に続きが得られるとは限りません。**長い回答が必要な質問は、最初から分割して投げる方が
  確実です**

出力上限は`models://registry`の`maxOutputTokens`で確認できます。**OpenAIモデル(`gpt-5.6-*`)
では推論(思考)トークンもこの上限に含まれる**ため、`effort`が高いほど本文に使える量が減ります。

### 選べるモデルID(`model`引数)

**このMCPサーバーはリソース`models://registry`を提供しています。**`resources/read`で
読み込めば、以下の表と同じ内容(コンテキスト長・画像対応可否・思考深さの対応段階・コスト
特性・データ共有有無を含むJSON)を、このガイドを都度読み直さずプログラム的に取得できます。
モデルが追加された場合もこのリソースは自動的に最新化されるため、正確な最新情報が必要な場合は
下表よりもこちらを優先してください。

| model ID | 実行先 | コスト | 備考 |
|---|---|---|---|
| (省略/`null`) → `google/gemma-4-12b-qat` | ローカル | **無料** | 既定モデル。迷ったらこれでよい |
| `gemma-4-e4b-uncensored-hauhaucs-aggressive` | ローカル | 無料 | フィルタ緩和モデル |
| `qwen/qwen3.8-27b` | ローカル | 無料 | 高精度・低速 |
| `gemini-3.8-flash` | Vertex AI | 課金対象(共有月$10枠) | 高速・長文脈(100万トークン超) |
| `xai/grok-4.6` | Vertex AI | 課金対象(共有月$10枠) | |
| `gpt-5.6-sol` | OpenAI(データ共有) | 日次無料枠25万トークン(単独) | 上限到達で自動的に使用不可になる |
| `gpt-5.6-terra` | OpenAI(データ共有) | 日次無料枠250万トークン(Lunaと共有) | |
| `gpt-5.6-luna` | OpenAI(データ共有) | 日次無料枠250万トークン(Terraと共有) | |

無効な`model`を指定した場合、有効なIDの一覧を含むエラーが返るので、それを見て修正すれば
よい(このリストが将来更新されてもエラーメッセージが常に最新の一覧を示す)。

### コスト・利用量に関する注意

クラウドモデル(`gemini-3.8-flash` / `xai/grok-4.6` / `gpt-5.6-*`)は、**Discord bot側の
人間の利用と同じコスト集計・日次無料枠を共有**します。

- **`gpt-5.6-*`(OpenAI)は、ユーザーのAPIキーがデータ共有(Data Sharing)プログラムを
  有効化した状態で使われています。** 送信したプロンプトはOpenAI側で学習等に利用される
  前提の代わりに、日次無料枠(Sol 25万トークン/日、Terra+Luna合算250万トークン/日)の
  範囲内であれば金銭的コストが一切発生しません。上限到達時は送信自体が自動的にブロック
  されるため、金銭コストが発生するリスクはありません
- 一方、`gemini-3.8-flash`/`xai/grok-4.6`(Vertex AI経由)には無料枠も自動ブロックもなく、
  利用の都度、共有の月$10枠(Google Developer Program)を静かに消費します

このため、**用途に応じてモデルを使い分けること**:
- タスクがローカルモデルで十分な場合は、まずローカルモデル(既定)を使う
- クラウドモデルが必要な場合は、金銭コストのリスクがない`gpt-5.6-*`(特にTerra/Lunaは
  日次無料枠が大きく余裕がある)を優先的に選ぶ
- `gemini-3.8-flash`の長文脈(100万トークン超)や、Gemini/Grok固有の挙動が明確に必要な
  場合に限り、Vertex AI系(`gemini-3.8-flash`/`xai/grok-4.6`)を選ぶ
- 機密性の高い内容を送る場合は、データ共有プログラム下にある`gpt-5.6-*`ではなく、
  ローカルモデルまたはVertex AI系を検討する

### 向いているタスク / 向いていないタスク

- 向いている: 独立した単発の質問、簡単なコードスニペット生成、定型的な調べ物・要約。
  `session_id`を使えば、単一のコーディング/監査タスク内での複数ターンの深掘り
  (「この差分をレビューして」→「2番目の指摘を詳しく」)にも対応可能
- 向いていない: セッションをまたいだ長期記憶が必要なタスク(20分・20往復で消える)、
  ファイル編集やコマンド実行などのツール実行が絡むタスク(このツールはテキスト生成のみで、
  モデル自体にツール実行能力を渡さない)、画像・添付ファイルを扱うタスク

## 提供ツール: `compare(prompt, models, effort?, temperature?)`

同じプロンプトを複数のモデルに順番に投げ、それぞれの応答を比較するツールです。
「この実装についてローカルモデルとGeminiの両方の意見を聞きたい」のような場面で使います。

### 引数

| 引数 | 必須 | 内容 |
|---|---|---|
| `prompt` | ○ | 全モデル共通の質問・依頼内容 |
| `models` | ○ | 比較したいモデルIDの配列(1個以上)。`model`引数と同じ値の集合。**空配列は不可** |
| `effort` | - | 思考の深さ。全モデル共通で適用(省略時Medium)。非対応の段階は自動調整される |
| `temperature` | - | 生成のランダム性。全モデル共通で適用(省略時0.7)。OpenAIモデルでは無視される |

`session_id`には対応していません(毎回独立したリクエスト)。複数モデル分の短期履歴を
個別に管理する複雑さに見合わないため、意図的に単発呼び出しのみに絞っています。

### レスポンス形式

各モデルの結果を含むJSON配列を文字列として返します:

```json
[
  { "modelId": "google/gemma-4-12b-qat", "answer": "...", "error": null, "warning": null, "finishReason": "stop", "durationMs": 2063, "estimatedCostUsd": null },
  { "modelId": "gpt-5.6-luna", "answer": "...", "error": null, "warning": "応答は出力上限に達して途中で切れています(...)。", "finishReason": "length", "durationMs": 95310, "estimatedCostUsd": 0.19 },
  { "modelId": "not-a-real-model", "answer": null, "error": "未知のモデルIDです。...", "warning": null, "finishReason": null, "durationMs": 0, "estimatedCostUsd": null }
]
```

**1モデルの失敗は他モデルの結果に影響しません。** 無効なモデルID・無料枠切れ・
コンテキスト超過・プロバイダー側のエラー・**本文が空の応答**は、そのモデルの`error`フィールドに
理由が入るだけで、ツール呼び出し自体は`isError`にならず、他モデルの結果は正常に返ります。

`finishReason`は各モデルの終了理由(`stop`/`STOP`が正常終了)です。本文はあるが出力上限等で
正常に書き終えなかった場合は`warning`に注記が入ります(`ask`の末尾注記と同じ内容)。
**`warning`が`null`でない回答は不完全な可能性がある**ものとして扱ってください。

### OpenAI(データ共有)モデルの扱い

`ask`の`model`引数と同じく、**比較対象にOpenAIモデル(`gpt-5.6-*`)を含めるかどうかは
呼び出し側の判断に委ねられており、ツール側での除外・警告は行いません。** 上記の
「コスト・利用量に関する注意」を踏まえ、機密性の高い内容を比較する場合は`models`配列に
`gpt-5.6-*`を含めないよう、呼び出し側で判断してください。

### 向いているタスク

同一プロンプトに対する複数モデルの回答を横断比較したい場合(セカンドオピニオン、
実装方針の妥当性確認、表現の揺れの確認等)。モデル数が多い・`effort`が高いと
その分時間がかかる点は`ask`と同様です(Progress notificationsで進捗を確認可能)。

## 制約

- ホストはRAM 1GBの小規模LXC上で、Discord bot本体と同一プロセス・同一リソースを共有して
  いる。大量の同時リクエストや過度に長いプロンプトは避けること
- `session_id`の履歴はプロセスメモリ上のみ(有効期限20分・最大20往復)。長期記憶や
  厳密な永続性が必要な情報はエージェント側で管理すること
- 応答は同期的に返る(ジョブ投げ→ポーリングのような非同期処理には未対応)。ただし呼び出し中は
  MCPプロトコルのProgress notifications(`notifications/progress`)を約3秒間隔で送信する
  ため、`tools/call`のリクエストに`progressToken`を含めて呼び出すMCPクライアントであれば、
  極端に長い応答生成でもタイムアウトが延長される可能性が高い(Claude Code等、対応クライアント
  側の挙動に依存)。実機検証では、ローカルモデル・`effort: High`で166秒かかった呼び出しも
  途中の進捗通知を挟みつつ正常完了することを確認済み

## 既知の注意点

- **日本語プロンプトの文字化け(クライアント側シェルの問題)**: MCPサーバー自体は日本語を
  正しく処理できます。ただし`curl`等でJSON-RPCリクエストを手動組み立ててテストする際、
  シェル環境によっては日本語プロンプトが文字化け(mojibake)した状態でサーバーに届く
  ことが確認されています(Git Bash環境で確認済み。原因はシェル側のエンコーディングで、
  サーバー側の不具合ではない)。手動でリクエストボディを構築する場合は、コマンドライン
  引数に直接埋め込む(`curl -d '...'`)のではなく、UTF-8で保存したファイルを
  `--data-binary @file`で送信すると正しく処理される。通常のMCPクライアント経由
  (`claude mcp add`で接続した状態でのツール呼び出し)ではこの問題は発生しない

## 困ったときは

- 接続できない: 自分の端末がTailscaleに参加しているか確認(`tailscale status`)
- ツール呼び出しがエラーを返す: エラーメッセージに理由が含まれる(無効なモデルID、
  OpenAI無料枠超過など)。文面に従って修正すれば再試行できる
- 応答が文字化けしている: 上記「既知の注意点」を参照(手動でHTTPリクエストを組み立てて
  いる場合、リクエストボディのエンコーディングを確認する)
